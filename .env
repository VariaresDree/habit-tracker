# Supabase connection for cross-device sync.
#
# This key is PUBLISHABLE and intentionally committed: the app is a static
# site, so anything it holds ships in the bundle and is readable by anyone.
# Security comes from Row Level Security on the server (every row is scoped
# to auth.uid()), not from hiding this string. Do not "fix" it into a secret.
VITE_SUPABASE_URL=https://oenjumiedjdgsfjkyivt.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_KAKGbflhgFD5cON7DCJajA_OdEshygS
